[build]
  base    = "."          # root of the frontend folder
  command = "npm run build"
  publish = "dist"

# Redirect all routes to index.html so React Router works on page refresh
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200